```mermaid
graph LR
  User[Пользователь] -->|Использует| UI[Графический интерфейс]
  UI -->|Обрабатывает запросы| Controller[Контроллер]
  Controller -->|Взаимодействует с базой данных| Database[База данных]
  Database -->|Возвращает данные| Controller
  Controller -->|Возвращает данные| UI
  UI -->|Отображает данные| User
```